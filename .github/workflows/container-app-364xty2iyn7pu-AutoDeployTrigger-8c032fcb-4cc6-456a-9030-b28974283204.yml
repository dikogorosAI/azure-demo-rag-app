name: Trigger auto deployment for container-app-364xty2iyn7pu

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-364xty2iyn7pu-AutoDeployTrigger-8c032fcb-4cc6-456a-9030-b28974283204.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP364XTY2IYN7PU_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP364XTY2IYN7PU_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP364XTY2IYN7PU_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP364XTY2IYN7PU_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP364XTY2IYN7PU_REGISTRY_PASSWORD }}
          containerAppName: container-app-364xty2iyn7pu
          resourceGroup: azuredemoragappaad3
          imageToBuild: default/container-app-364xty2iyn7pu:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


